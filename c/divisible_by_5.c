#include <stdio.h>

void check_divisibility(int num) {
    // if(num%5==0)
    {
        print("this number is divisble by 5");
    }
}

int main() {
    int num;
    scanf("%d", &num);
    check_divisibility(num);
    return 0;
}
